package game;

import board.*;
import enums.Colour;

public class Game {

	Controller controller;
	
	public Game() {
		//controller = new Controller(new Board(), new Player(Colour.WHITE), new Player(Colour.BLACK));
	}
}
